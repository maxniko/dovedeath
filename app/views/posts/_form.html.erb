<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :cuerpo %><br />
    <%= f.text_area :cuerpo %>
  </div>
  <div class="field">

<%= f.select(:categoria_id, @categoria, {:prompt => "#{t "categoria.vista.form.por_defecto"}",
                                           :order => "nombre"} ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

